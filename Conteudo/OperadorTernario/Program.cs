using System.Data;
using System.Diagnostics.Tracing;

namespace OperadorTernario
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Operador Ternário

            //DateTime agora = DateTime.Now;
            //Console.WriteLine(agora.ToLongDateString() + " " + agora.ToLongTimeString());

            //string saudacao;
            //variavel = teste ? "atribui isso se for verdade" : "se for falso esse";
            //saudacao = agora.Hour > 12 ? "Boa Tarde" : "Bom dia";
            //Console.WriteLine(saudacao + "!");
            //Console.WriteLine("Agora são " + agora.Hour + " horas e " + agora.Minute + " minutos e " + agora.Second + "segundos.");
            //Console.WriteLine("Hoje é dia " + agora.Day + " de " + agora.Month + " de " + agora.Year);

            //int n = 0;
            //string testa = n == 0 ? "N é igual a zero" : "N é diferente de zero";
            //Console.WriteLine("A presenta resultado: " + testa);

            //---------------------------------------------------------------------------------------------------------

            //Exemplo2: escreva um algoritmo que mostre todos os numeros de 1 a 10
            //for(int i = 0; i <=10; i++)
            //{
            //    Console.WriteLine(i);
            //}
            //---------------------------------------------------------------------------------------------------------
            //Exemplo 3: Escreva um algoritmo que mostre todos os numeros de 20 a 5
            //for (int i = 20; i >= 5; i--)
            //{
            //    Console.WriteLine(i);
            //}
            //-----------------------------------------------------------------------------------------------
            //Exemplo 4: Escreva um algoritmo que leia pelo teclado os dois valores interiros chamados inicio e fim
            //, e então mostre todos os números entre inicio e fim.
            //Console.WriteLine("Informe um valor: ");
            //int inicio = int.Parse(Console.ReadLine());
            //Console.WriteLine("Informe outro valor: ");
            //int fim = int.Parse(Console.ReadLine());
            //if (inicio < fim)
            //{
            //    for (int i = inicio + 1; i < fim; i++)
            //    {
            //        Console.WriteLine(i);
            //    }
            //}
            //else
            //{
            //    for (int i = fim - 1; i < inicio; i++)
            //    {
            //        Console.WriteLine(i);
            //    }
            //}
            //-----------------------------------------------------------------------------------------------
            //Exemplo 5: Escreva um algoritmo que leia pelo teclado um valor inteiro chamado x, e então mostre
            //na tela todos os numero de 0 a 100 pulando de x em x
            //Console.WriteLine("Informe um número: ");
            //int num = int.Parse(Console.ReadLine());

            //for(int i = 0; i <=100; i+=num)
            //{
            //    Console.WriteLine(i);
            //}
            //-----------------------------------------------------------------------------------------------
            //Exemplo 6: Escreva um algoritmo que leia o valor de um inteiro chamado x, e então mostre todos 
            //os numeros de 0 a 100 que são divisiveis por x.
            //Console.WriteLine("Informe um valor X: ");
            //int x = int.Parse(Console.ReadLine());
            //for(int i = 0; i < 100; i++)
            //{
            //    if(i % x == 0)
            //    {
            //        Console.WriteLine(i);
            //    }
            //}
            //-----------------------------------------------------------------------------------------------

            //while(true)
            //{
            //    Console.WriteLine("Digite um valor para OP: ");
            //    int op = int.Parse(Console.ReadLine());
            //    if(op == 0)
            //    {
            //        break;
            //    }
            //}
        }

    }
}